# Test environment — placeholder values so config.ts doesn't throw.
# Tests use mock providers and never call real Gemini or Android.
GEMINI_API_KEY=test-key-placeholder
GEMINI_MODEL=gemini-2.0-flash
PORT=0
HOST=127.0.0.1
ANDROID_HOST=127.0.0.1
ANDROID_PORT=5555
MAX_LOCAL_TOKENS=1500
