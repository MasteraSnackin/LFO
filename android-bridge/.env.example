# android-bridge environment variables
# Copy to .env and fill in your values, or set these in your shell before running.

# Port this bridge listens on (must match ANDROID_PORT in lfo-core .env)
BRIDGE_PORT=5555

# LAN IP address of the Android device running lfo-mobile
# Find with: adb shell ip route
ANDROID_HOST=192.168.0.50

# TCP port the lfo-mobile app listens on (default 6000, set in lfo-mobile/src/server.ts)
ANDROID_PORT=6000

# Milliseconds before a call to the Android device is considered timed out
ANDROID_TIMEOUT_MS=30000
